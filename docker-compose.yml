services:
  postgres:
    image: postgres:16-alpine
    container_name: visionrag-postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: visionrag
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:6.2-alpine
    container_name: visionrag-redis
    restart: always
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: visionrag-rabbitmq
    restart: always
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    ports:
      - "5673:5672"
      - "15673:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq

  gateway-service:
    build: 
      context: .
      dockerfile: services/GatewayServiceGo/Dockerfile
    container_name: visionrag-gateway
    restart: always
    ports:
      - "9090:9000"
    environment:
      - PUBLIC_SERVICE_URL=http://public-service:9090
      - CHAT_SERVICE_URL=http://chat-service:9092
    depends_on:
      - public-service
      - chat-service

  public-service:
    build: 
      context: .
      dockerfile: services/PublicServiceGo/Dockerfile
    container_name: visionrag-public
    restart: always
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=password
      - DB_NAME=visionrag
      - DB_TIMEZONE=UTC
      - REDIS_HOST=redis
      - RABBITMQ_HOST=rabbitmq
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  chat-service:
    build: 
      context: .
      dockerfile: services/ChatServiceGo/Dockerfile
    container_name: visionrag-chat
    restart: always
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=password
      - DB_NAME=visionrag
      - DB_TIMEZONE=UTC
      - REDIS_HOST=redis
      - RABBITMQ_HOST=rabbitmq
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_started

volumes:
  postgres_data:
  redis_data:
  rabbitmq_data:
